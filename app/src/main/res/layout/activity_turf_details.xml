<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".TurfDetailActivity">

    <ImageView
        android:id="@+id/turfImageView"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        android:src="@drawable/placeholder_image" />

    <TextView
        android:id="@+id/turfNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/turfImageView"
        android:layout_marginTop="16dp"
        android:text="Turf Name"
        android:textSize="24sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/turfLocationTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/turfNameTextView"
        android:layout_marginTop="8dp"
        android:text="Turf Location"
        android:textSize="18sp" />

    <!-- Date selection -->
    <LinearLayout
        android:id="@+id/dateLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/turfLocationTextView"
        android:layout_marginTop="16dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnSelectDate"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:onClick="onSelectDateClick"
            android:text="Select Date"
            tools:ignore="OnClick" />

    </LinearLayout>

    <!-- Start time and end time selection -->
    <LinearLayout
        android:id="@+id/timeLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/dateLayout"
        android:layout_marginTop="16dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnSelectStartTime"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:onClick="onSelectStartTimeClick"
            android:text="Select Start Time"
            android:textSize="12sp"
            tools:ignore="OnClick" />

        <Button
            android:id="@+id/btnSelectEndTime"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="16dp"
            android:onClick="onSelectEndTimeClick"
            android:text="Select End Time"
            tools:ignore="OnClick" />
    </LinearLayout>

    <com.paypal.checkout.paymentbutton.PaymentButtonContainer
        android:id="@+id/payment_button_container"
        android:layout_width="382dp"
        android:layout_height="69dp"
        android:layout_below="@id/timeLayout"
        android:layout_marginTop="34dp"
        android:text="Book"
        app:layout_constraintTop_toBottomOf="@id/timeLayout"
        tools:ignore="NotSibling" />

</RelativeLayout>
