<resources>

    <!-- Theme without ActionBar for LoginActivity -->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Custom style for TurfCard item -->
    <style name="TurfCardStyle">
        <!-- Define styles for TurfCard items here -->
        <item name="android:background">@drawable/turf_card_background</item>
        <!-- Add other styles as needed -->
    </style>
    <!-- res/values/styles.xml -->
    <style name="App_theme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Your other style attributes -->

        <!-- Add this line to define the missing style -->
        <item name="popupTheme">@style/AppTheme.PopupOverlay</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar"/>
    <style name="NoActionBarTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- No action bar -->
        <item name="android:windowBackground">@drawable/background_splash</item> <!-- Set splash background -->
        <item name="android:windowAnimationStyle">@style/FadeInAnimation</item> <!-- Add animation -->
        <item name="android:statusBarColor">@color/your_background_color</item> <!-- Status bar color -->
        <item name="colorPrimary">@color/primary_color</item> <!-- Material color -->
        <item name="colorPrimaryDark">@color/primary_dark_color</item> <!-- Material color for status bar -->
        <item name="colorAccent">@color/accent_color</item> <!-- Accent color -->
        <item name="android:navigationBarColor">@color/white</item>
    </style>

    <!-- Splash screen fade in animation -->
    <style name="FadeInAnimation">
        <item name="android:windowEnterAnimation">@anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>


</resources>
